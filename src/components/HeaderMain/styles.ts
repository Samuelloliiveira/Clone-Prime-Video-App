import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  background: {
    paddingTop: 60,
    paddingLeft: 20,
    paddingBottom: 20,
    backgroundColor: "#0D1821"
  },
})